import { ContractsFilterPipe } from './contracts-filter.pipe';

describe('ContractFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
